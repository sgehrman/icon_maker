name: icon_maker
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  barcode_widget: ^2.0.4
  dfc_flutter:
    git: https://github.com/sgehrman/dfc_flutter
  file_selector: ^0.9.2+4
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.2
  font_awesome_flutter: ^10.4.0
  google_fonts: ^4.0.4
  image: ^4.0.15
  jovial_svg: ^1.1.12

dev_dependencies:
  dart_code_metrics: ^5.4.0
  dfc_lints:
    git: https://github.com/sgehrman/dfc_lints
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/screenshots/
    - assets/wallpaper/

  shaders:
    # - shaders/pyramid.glsl
    # - shaders/warp.glsl
    # - shaders/triangles.glsl
    # - shaders/nebula.glsl
    # - shaders/dmt.glsl
    # - shaders/boreal.glsl
    - shaders/coloredCells.glsl
    # - shaders/hyperboloid.glsl
    - shaders/starTunnel.glsl
    # - shaders/starfield.glsl
